import React from 'react'

const StepAvator = ({onNext}) => {
    return (
        <>
           <div>Avator Component</div>
            <button onClick={onNext}>Next</button>
        </>
    )
}

export default StepAvator
